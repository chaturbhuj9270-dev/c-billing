import 'package:get_it/get_it.dart';

final GetIt sl = GetIt.instance;

Future<void> init() async {
  // Register core services here (e.g., network, logger)
  // Feature registrations should be done here or in separate modules.
}
